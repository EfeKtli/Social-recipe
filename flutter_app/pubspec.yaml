name: flutter_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^6.0.0
  provider: ^6.0.2
  # Diğer bağımlılıkları buraya ekleyin
  cupertino_icons: ^1.0.8
  google_fonts: ^5.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package enforces good coding practices.
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/